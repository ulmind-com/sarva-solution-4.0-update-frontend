# Phone OTP verification on registration.
# Defaults to on. Set to "false" to let members sign up without an SMS OTP
# (e.g. while the client is testing, so no SMS cost is incurred).
# Must be set together with OTP_VERIFICATION_REQUIRED on the backend.
VITE_OTP_VERIFICATION_REQUIRED=true
